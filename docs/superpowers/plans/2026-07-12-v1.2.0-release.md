# Study Buddy 1.2.0 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将后端、UniApp 客户端、管理后台和 Docker 镜像版本统一为 1.2.0，并发布准确的 Docker 配置、CHANGELOG 和 README。

**Architecture:** 后端运行时以 `app/core/version.py` 的 `APP_VERSION` 为唯一版本来源，FastAPI 和健康检查从该常量读取。各打包生态保留其必须的版本字段，并通过自动化测试确保全部等于 1.2.0；Docker 镜像通过构建参数和 OCI 标签暴露同一版本，Compose 使用版本化镜像名和稳定容器名。

**Tech Stack:** Python 3.11、FastAPI、pytest、UniApp/Vue 3/TypeScript、npm、Dockerfile、Docker Compose、Markdown

## Global Constraints

- 发布版本固定为 `1.2.0`，发布日期固定为 `2026-07-12`。
- UniApp 使用 `versionName: "1.2.0"` 和 `versionCode: "120"`。
- Docker 镜像名固定为 `studybuddy-api:1.2.0`，容器名固定为 `studybuddy-api`。
- Docker 继续只包含 API 和已提交的 `static/admin-dist`；不构建或打包移动端客户端。
- Compose 继续连接外部 MySQL，不新增数据库服务。
- 本次不创建 Git 标签或 GitHub Release。
- Git commit message 必须使用中文。
- 当前机器没有 Docker CLI；只能运行静态 Docker 测试，不得声称镜像构建或容器运行通过。

---

## File Map

- Create `app/core/version.py`: 后端运行时版本唯一来源。
- Modify `app/main.py`: FastAPI/OpenAPI 与 `/health` 暴露 1.2.0。
- Modify `client/src/manifest.json`: UniApp 安装包版本。
- Modify `client/package.json`, `client/package-lock.json`: 客户端 npm 元数据版本。
- Modify `admin/package.json`, `admin/package-lock.json`: 管理后台 npm 元数据版本。
- Create `tests/test_release_version.py`: 跨生态版本一致性契约。
- Modify `Dockerfile`: 去除重复依赖安装并写入 OCI 版本标签。
- Modify `docker-compose.yml`: 版本化镜像、稳定容器名、健康检查。
- Modify `tests/test_docker_compose.py`: Compose 版本、容器名和健康检查契约。
- Modify `tests/test_docker_runtime_config.py`: Dockerfile 安装与镜像标签契约。
- Modify `CHANGELOG.md`: 1.2.0 发布说明与当前 MySQL 部署示例。
- Modify `README.md`: 当前版本、周期作业功能、API 与 Docker 升级说明。

---

### Task 1: 建立并统一 1.2.0 版本契约

**Files:**
- Create: `app/core/version.py`
- Modify: `app/main.py:1-54`
- Modify: `client/src/manifest.json:1-6`
- Modify: `client/package.json:1-3`
- Modify: `client/package-lock.json:1-10`
- Modify: `admin/package.json:1-4`
- Modify: `admin/package-lock.json:1-12`
- Create: `tests/test_release_version.py`

**Interfaces:**
- Produces: `app.core.version.APP_VERSION: str = "1.2.0"`。
- Produces: `GET /health -> {"status": "ok", "version": "1.2.0"}`。
- Produces: 所有客户端/管理后台版本元数据均为 `1.2.0`；UniApp `versionCode` 为字符串 `"120"`。

- [ ] **Step 1: 写失败的跨生态版本测试**

创建 `tests/test_release_version.py`：

```python
import json
import re
from pathlib import Path

from fastapi.testclient import TestClient

from app.core.version import APP_VERSION
from app.main import app

EXPECTED_VERSION = "1.2.0"


def read_json_with_comments(path: str) -> dict:
    text = Path(path).read_text(encoding="utf-8")
    text = re.sub(r"/\*.*?\*/", "", text, flags=re.DOTALL)
    text = re.sub(r"//.*", "", text)
    return json.loads(text)


def test_runtime_version_and_health_endpoint_are_current():
    assert APP_VERSION == EXPECTED_VERSION
    assert app.version == EXPECTED_VERSION
    response = TestClient(app).get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok", "version": EXPECTED_VERSION}


def test_frontend_package_versions_match_release():
    client_manifest = read_json_with_comments("client/src/manifest.json")
    client_package = json.loads(Path("client/package.json").read_text(encoding="utf-8"))
    client_lock = json.loads(Path("client/package-lock.json").read_text(encoding="utf-8"))
    admin_package = json.loads(Path("admin/package.json").read_text(encoding="utf-8"))
    admin_lock = json.loads(Path("admin/package-lock.json").read_text(encoding="utf-8"))

    assert client_manifest["versionName"] == EXPECTED_VERSION
    assert client_manifest["versionCode"] == "120"
    for package in (client_package, client_lock, admin_package, admin_lock):
        assert package["version"] == EXPECTED_VERSION
    assert client_lock["packages"][""]["version"] == EXPECTED_VERSION
    assert admin_lock["packages"][""]["version"] == EXPECTED_VERSION
```

- [ ] **Step 2: 运行测试并确认因版本实现缺失而失败**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py -v
```

Expected: FAIL，最先出现 `ModuleNotFoundError: No module named 'app.core.version'`。

- [ ] **Step 3: 新增后端版本常量并接入 FastAPI**

创建 `app/core/version.py`：

```python
APP_VERSION = "1.2.0"
```

修改 `app/main.py`：

```python
from app.core.version import APP_VERSION

app = FastAPI(title="作业陪伴助手", version=APP_VERSION, lifespan=lifespan)

@app.get("/health")
def health_check():
    return {"status": "ok", "version": APP_VERSION}
```

保留现有中间件、路由、lifespan 和静态文件挂载逻辑不变。

- [ ] **Step 4: 同步 UniApp、客户端和管理后台版本字段**

修改 `client/src/manifest.json`：

```json
"versionName" : "1.2.0",
"versionCode" : "120"
```

修改 `client/package.json` 和 `admin/package.json` 顶层字段：

```json
"version": "1.2.0"
```

使用 npm 只更新 lockfile 元数据而不安装依赖：

```bash
npm --prefix client version 1.2.0 --no-git-tag-version --allow-same-version
npm --prefix admin version 1.2.0 --no-git-tag-version --allow-same-version
```

检查命令只改变对应 package/lockfile 的顶层版本及根 package 版本，不接受依赖树无关变动。

- [ ] **Step 5: 运行版本测试并确认通过**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py -v
```

Expected: `2 passed`。

- [ ] **Step 6: 运行受影响后端与前端元数据检查**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py tests/test_auth.py -q
npm --prefix client run type-check
npm --prefix admin run build
```

Expected: pytest 全部通过、两个 npm 命令退出码为 0。若管理后台构建改变 `static/admin-dist`，用 `git status --short static/admin-dist` 检查；仅构建哈希因相同源码稳定变化时不应提交产物，否则核对构建产物是否确属源码变化。

- [ ] **Step 7: 提交版本契约**

```bash
git add app/core/version.py app/main.py client/src/manifest.json client/package.json client/package-lock.json admin/package.json admin/package-lock.json tests/test_release_version.py
git commit -m "统一项目版本为 1.2.0"
```

---

### Task 2: 修正并验证 Docker 发布配置

**Files:**
- Modify: `Dockerfile:1-27`
- Modify: `docker-compose.yml:1-14`
- Modify: `tests/test_docker_compose.py`
- Modify: `tests/test_docker_runtime_config.py`

**Interfaces:**
- Consumes: `APP_VERSION = "1.2.0"` 发布契约。
- Produces: Docker build arg `APP_VERSION`，镜像环境变量 `APP_VERSION`，OCI 标签 `org.opencontainers.image.version`。
- Produces: Compose image `studybuddy-api:1.2.0`、container `studybuddy-api`、`service_healthy` 状态检测。

- [ ] **Step 1: 写失败的 Docker 静态契约测试**

在 `tests/test_docker_compose.py` 增加：

```python
def test_compose_uses_release_image_stable_name_and_healthcheck():
    compose = Path("docker-compose.yml").read_text(encoding="utf-8")

    assert "image: studybuddy-api:1.2.0" in compose
    assert "container_name: studybuddy-api\n" in compose
    assert "studybuddy-api-2.0" not in compose
    assert "healthcheck:" in compose
    assert "http://127.0.0.1:8000/health" in compose
```

在 `tests/test_docker_runtime_config.py` 增加：

```python
def test_dockerfile_uses_requirements_once_and_labels_release_version():
    dockerfile = Path("Dockerfile").read_text(encoding="utf-8")

    assert "ARG APP_VERSION=1.2.0" in dockerfile
    assert "ENV APP_VERSION=$APP_VERSION" in dockerfile
    assert 'org.opencontainers.image.version="$APP_VERSION"' in dockerfile
    assert "pip install --no-cache-dir -r requirements.txt\n" in dockerfile
    assert "-r requirements.txt gunicorn" not in dockerfile
```

- [ ] **Step 2: 运行 Docker 静态测试并确认失败**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_docker_compose.py tests/test_docker_runtime_config.py -v
```

Expected: 新测试因旧容器名、缺失 image/healthcheck/OCI 标签和重复 pip 参数而 FAIL。

- [ ] **Step 3: 最小化修正 Dockerfile**

将 Dockerfile 调整为：

```dockerfile
FROM python:3.11-slim

ARG APP_VERSION=1.2.0
ENV APP_VERSION=$APP_VERSION
LABEL org.opencontainers.image.title="Study Buddy API" \
      org.opencontainers.image.version="$APP_VERSION"

WORKDIR /app

RUN pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
RUN chmod +x docker-entrypoint.sh \
    && mkdir -p /app/tmp /app/logs \
    && useradd -m appuser \
    && chown -R appuser:appuser /app

USER appuser
EXPOSE 8000
ENTRYPOINT ["./docker-entrypoint.sh"]
```

不要改变 entrypoint 启动命令、工作目录或非 root 用户策略。

- [ ] **Step 4: 更新 Compose 镜像、容器名与健康检查**

将 `docker-compose.yml` 改为：

```yaml
services:
  studybuddy-api:
    build:
      context: .
      args:
        APP_VERSION: "1.2.0"
    image: studybuddy-api:1.2.0
    container_name: studybuddy-api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /data/logs/studybuddy:/app/logs
      - ./tmp:/app/tmp
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
```

- [ ] **Step 5: 运行 Docker 静态测试并确认通过**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_docker_compose.py tests/test_docker_runtime_config.py -v
```

Expected: 全部通过。

- [ ] **Step 6: 记录 Docker CLI 验证限制**

Run:

```bash
docker compose config
```

Expected in current environment: command unavailable (`docker: command not found`)。不要把它当作代码失败；在最终验证报告中明确记录“未执行实际 Compose 解析与镜像构建”。如果执行环境后来提供 Docker，则要求 `docker compose config` 和 `docker build --build-arg APP_VERSION=1.2.0 -t studybuddy-api:1.2.0 .` 均退出码为 0。

- [ ] **Step 7: 提交 Docker 配置**

```bash
git add Dockerfile docker-compose.yml tests/test_docker_compose.py tests/test_docker_runtime_config.py
git commit -m "更新 1.2.0 Docker 发布配置"
```

---

### Task 3: 发布 1.2.0 更新日志并更新 README

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Test: `tests/test_release_version.py`

**Interfaces:**
- Consumes: 版本 `1.2.0`、镜像 `studybuddy-api:1.2.0`、健康检查 `/health`。
- Produces: 面向使用者和运维者的 1.2.0 发布说明及升级步骤。

- [ ] **Step 1: 为发布文档写失败测试**

在 `tests/test_release_version.py` 增加：

```python
def test_release_docs_describe_current_version_and_docker_upgrade():
    changelog = Path("CHANGELOG.md").read_text(encoding="utf-8")
    readme = Path("README.md").read_text(encoding="utf-8")

    assert "## [1.2.0] - 2026-07-12" in changelog
    assert "周期作业计划" in changelog
    assert "当前版本：**1.2.0**" in readme
    assert "studybuddy-api:1.2.0" in readme
    assert "docker compose ps" in readme
    assert "GET /api/v1/homework-plans/" in readme
```

- [ ] **Step 2: 运行发布文档测试并确认失败**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py::test_release_docs_describe_current_version_and_docker_upgrade -v
```

Expected: FAIL，提示 CHANGELOG 缺少 `1.2.0` 标题。

- [ ] **Step 3: 在 CHANGELOG 顶部新增 1.2.0 发布说明**

在 `# 更新日志` 下方加入：

```markdown
## [1.2.0] - 2026-07-12

### 新功能

- 新增周期作业计划，支持最近一周、最近一个月和自定义日期范围。
- 支持计划自动物化、缺失日期补生成、编辑未来任务模板和停止后续生成。
- 家长端新增活动计划卡片、听写预览、错题本入口和完整批改流程。
- 学生端支持计划任务同步、连续听写、提交状态和家长评价展示。

### 优化

- 发布页和编辑页统一使用听写配置组件。
- 作业卡片清理重复日期标题，计划操作采用统一有机主题样式。
- 注册流程直接调用注册接口，避免预期外的登录请求。
- Docker 镜像、运行时、客户端和管理后台版本统一为 1.2.0。

### 修复

- 修复 Windows 缺少 IANA 时区数据导致作业计划接口返回 500 的问题。
- 修复听写完成后显示“第 0 个”及已提交/已批改任务被重置为进行中的问题。
- 修复批改弹窗“跳过”未完成评分的问题。
- 修复周期作业计划并发生成、编辑和删除时的一致性问题。

### 部署变更

- Docker 镜像名为 `studybuddy-api:1.2.0`，容器名稳定为 `studybuddy-api`。
- Compose 增加 `/health` 健康检查。
- Windows 与最小化 Linux 环境通过 `tzdata` 依赖提供 `Asia/Shanghai` 时区数据。
```

同时将旧“部署说明”中的 PostgreSQL 示例改为当前 MySQL：

```env
DATABASE_URL=mysql+pymysql://studybuddy:请替换为强密码@host.docker.internal:3306/studybuddy
```

- [ ] **Step 4: 更新 README 当前版本与功能描述**

在项目介绍后加入：

```markdown
当前版本：**1.2.0**
```

在功能列表加入：

```markdown
- **周期作业计划**：按周、月或自定义日期每天生成任务，支持补生成、编辑未来任务和停止计划
```

更新项目结构中的 API 说明，使 `homework_plans` 和 materializer 可见；保持已有目录树风格，不扩展无关目录。

- [ ] **Step 5: 更新 README API 概览**

在用户端 API 表加入：

```markdown
| 作业计划 | `POST /api/v1/homework-plans/` | 创建周期作业计划 |
| | `GET /api/v1/homework-plans/` | 查询当前家庭活动计划 |
| | `GET /api/v1/homework-plans/{id}` | 获取计划详情 |
| | `PATCH /api/v1/homework-plans/{id}` | 更新未来任务模板 |
| | `DELETE /api/v1/homework-plans/{id}` | 停止计划后续生成 |
| | `POST /api/v1/homework-plans/materialize` | 补生成截至今天的计划任务 |
```

- [ ] **Step 6: 更新 README Docker 部署与升级说明**

Docker 章节必须明确包含：

```markdown
docker compose up -d --build
docker compose ps
curl http://localhost:8000/health
```

预期健康响应：

```json
{"status":"ok","version":"1.2.0"}
```

加入从旧版本升级：

```bash
git pull
pip install -r requirements.txt   # 非 Docker 部署
docker compose down
docker compose up -d --build
docker compose ps
```

说明镜像名是 `studybuddy-api:1.2.0`，后端镜像包含 API 与 `static/admin-dist`，UniApp 客户端需要从 `client/` 单独构建。

- [ ] **Step 7: 运行文档契约测试并确认通过**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py -v
```

Expected: `3 passed`。

- [ ] **Step 8: 提交发布文档**

```bash
git add CHANGELOG.md README.md tests/test_release_version.py
git commit -m "发布 1.2.0 更新日志与部署说明"
```

---

### Task 4: 完整验证、审查并推送发布文件

**Files:**
- Verify all files from Tasks 1-3.
- Do not modify or stage `.claude/launch.json` and `.claude/settings.local.json`.

**Interfaces:**
- Consumes: Tasks 1-3 的全部发布契约。
- Produces: 已验证并推送的 1.2.0 发布文件，不含 Git 标签/GitHub Release。

- [ ] **Step 1: 运行后端完整测试**

Run:

```bash
.venv/Scripts/python -m pytest tests/ -q
```

Expected: 0 failures；允许现有依赖 MySQL 的并发测试保持 skip，但必须报告实际 pass/skip 数量。

- [ ] **Step 2: 运行客户端完整验证**

Run:

```bash
npm --prefix client run type-check
npm --prefix client test -- --run
```

Expected: 两条命令退出码为 0，全部前端测试通过；Sass legacy JS API 警告可记录为非阻断警告。

- [ ] **Step 3: 运行管理后台构建**

Run:

```bash
npm --prefix admin run build
```

Expected: 退出码为 0。随后运行：

```bash
git status --short static/admin-dist
```

Expected: 无意外产物变化；如果构建产物变化，核对是否仅因 package version 进入 bundle。没有运行时版本展示需求时，不提交无关哈希变化。

- [ ] **Step 4: 检查发布契约与 diff 完整性**

Run:

```bash
.venv/Scripts/python -m pytest tests/test_release_version.py tests/test_docker_compose.py tests/test_docker_runtime_config.py -v
git diff --check
git status --short
git log -4 --oneline
```

Expected: 测试全部通过；`git diff --check` 无错误；工作区只剩用户原有 `.claude/launch.json` 与 `.claude/settings.local.json` 修改。

- [ ] **Step 5: 检查提交内容不含秘密或本地配置**

Run:

```bash
git show --stat --oneline HEAD~2..HEAD
git diff origin/dev/1.2/zrf..HEAD -- . ':(exclude).claude/launch.json' ':(exclude).claude/settings.local.json'
```

检查要求：不得出现 JWT、真实密码、API key、`.env` 内容；不得提交 `.claude/settings.local.json`；不得把 `prototype-static` 本地 launch 配置带入发布提交。

- [ ] **Step 6: 推送当前分支，不创建标签**

Run:

```bash
git push origin dev/1.2/zrf
```

Expected: 当前 HEAD 推送到 `origin/dev/1.2/zrf`。不要执行 `git tag` 或 `gh release create`。

- [ ] **Step 7: 报告发布结果与 Docker 限制**

最终报告必须包含：

- 版本已统一到 1.2.0 的文件列表。
- Dockerfile/Compose 的实际修改及理由。
- CHANGELOG/README 更新摘要。
- 后端、客户端、管理后台的实际验证结果。
- 明确写出：当前机器没有 Docker CLI，未执行镜像构建或容器运行验证。
- 推送的分支和提交哈希。
